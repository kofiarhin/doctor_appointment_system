{
  "name": "doctor-appointment-system",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "npm --prefix client/details run dev",
    "dev:server": "npm --prefix server/server-details run dev",
    "install:all": "npm install && npm --prefix client/details install && npm --prefix server/server-details install",
    "test": "npm --prefix client/details run test && npm --prefix server/server-details run test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
