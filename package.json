{
  "name": "doctor-appointment-system-tests",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test:frontend": "vitest run --coverage",
    "test:backend": "jest --coverage",
    "test": "npm run test:frontend && npm run test:backend"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "jest": "^29.7.0",
    "sass": "^1.83.4",
    "vitest": "^2.1.8"
  }
}
